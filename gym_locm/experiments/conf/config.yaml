defaults:
  - _self_
  - model: set_transformer
  - adversary: fixed

# Global settings
version: "1.5"
task: "battle"
seed: 42
train_episodes: 250000
eval_episodes: 50
num_evals: 250
concurrency: 2

# Reward functions
reward_functions: "win-loss"
reward_weights: "1"
use_average_deck: true

# Logging & Paths
no_tracking: true
wandb_entity: "ronaldosvieira"
wandb_project: "tese"
path: "gym_locm/experiments/papers/tese/battle-2026-05-28-1400"
